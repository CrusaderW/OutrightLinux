#!/bin/bash
#TODO check if git is installed: if not: do so...
git config --global user.email "wirth@protectivecircle.com"
git config --global user.name "Christian Wirth"
git config --global push.default simple
